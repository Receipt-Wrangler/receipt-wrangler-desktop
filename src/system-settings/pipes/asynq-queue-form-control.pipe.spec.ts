import { AsynqQueueFormControlPipe } from './asynq-queue-form-control.pipe';

describe('AsynqQueueFormControlPipe', () => {
  it('create an instance', () => {
    const pipe = new AsynqQueueFormControlPipe();
    expect(pipe).toBeTruthy();
  });
});
