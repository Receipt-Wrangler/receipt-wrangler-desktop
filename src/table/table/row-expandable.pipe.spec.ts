import { RowExpandablePipe } from './row-expandable.pipe';

describe('RowExpandablePipe', () => {
  it('create an instance', () => {
    const pipe = new RowExpandablePipe();
    expect(pipe).toBeTruthy();
  });
});
