import { AllItemsResolvedPipe } from './all-items-resolved.pipe';

describe('AllItemsResolvedPipe', () => {
  it('create an instance', () => {
    const pipe = new AllItemsResolvedPipe();
    expect(pipe).toBeTruthy();
  });
});
