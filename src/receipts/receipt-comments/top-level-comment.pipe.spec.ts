import { TopLevelCommentPipe } from './top-level-comment.pipe';

describe('TopLevelCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new TopLevelCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
