import { SymbolPositionPipe } from './symbol-position.pipe';

describe('SymbolPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new SymbolPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
