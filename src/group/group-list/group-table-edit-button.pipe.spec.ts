import { GroupTableEditButtonPipe } from './group-table-edit-button.pipe';

describe('GroupTableEditButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupTableEditButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
